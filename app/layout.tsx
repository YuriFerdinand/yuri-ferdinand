import type { Metadata, Viewport } from "next";
import { Geist, Cormorant_Garamond } from "next/font/google";
import "./globals.css";
import ClientWrapper from "@/components/ClientWrapper";

const jakartaSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  display:"swap"
});
const cormorantGaramond = Cormorant_Garamond({
  variable: "--font-cormorant-garamond",
  subsets: ["latin"],
  display:"swap"
});

export const metadata: Metadata = {
  title: "Yuri Ferdinand",
  description: "Generated by create next app",
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  userScalable: false,
};

export default function RootLayout({children,}: Readonly<{children: React.ReactNode;}>) {
  
  return (
    <html lang="en" className={`${cormorantGaramond.variable} ${jakartaSans.variable}`}>
      <body
        className={`overflow-x-hidden text-[#e8e6e0] antialiased container mx-auto font-body bg-[#0f0f0f]`}
      >
        <ClientWrapper>
          {children}
        </ClientWrapper>
      </body>
    </html>
  );
}
